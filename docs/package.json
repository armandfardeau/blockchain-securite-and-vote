{
  "dependencies": {
    "gitbook-cli": "^2.3.2",
    "svgexport": "^0.3.2"
  },
  "scripts": {
    "plugins": "gitbook install",
    "pdf": "gitbook pdf",
    "serve": "gitbook serve",
    "build": "yarn run pdf && gitbook build && rm -r docs && mkdir docs && cp -r _book/* docs/"
  }
}
